{
  "name": "onda-ritual",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-types": "wrangler types"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
